/**
 * @author: Isaias :D
 * En este archivo se establece la concexion a la DB
 */

 /*
 const {Client} = require('pg');

 const connectionData = {
    user: 'Usuario',
    host: '',
    database: '',
    password: '',
    port: 1234,
 }

 const client = new Client( connectionData );

 client.connect()
client.query('SELECT * FROM table')
    .then(response => {
        console.log(response.rows)
        client.end()
    })
    .catch(err => {
        client.end()
    })

module.exports = Client;

*/
 